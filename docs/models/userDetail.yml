definitions:
  UserDetail:
    type: object
    properties:
      id:
        type: integer
      token:
        type: string
      username:
        type: string
      first_name:
        type: string
      last_name:
        type: string
      is_active:
        type: boolean
      send_feedback:
        type: boolean
      email:
        type: string
      roles:
        $ref: '#/definitions/Role'
      approver:
        $ref: '#/definitions/User'

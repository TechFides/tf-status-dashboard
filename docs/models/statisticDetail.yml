definitions:
  Statistic:
    type: object
    properties:
      standups:
        $ref: '#/definitions/Standup'
      jiraSynchronization:
        $ref: '#/definitions/JiraSynchronization'
      userStatistic:
        $ref: '#/definitions/UserStatistic'

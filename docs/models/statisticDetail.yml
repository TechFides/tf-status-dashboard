definitions:
  Statistic:
    type: object
    properties:
      sitdowns:
        $ref: '#/definitions/Sitdown'
      jiraSynchronization:
        $ref: '#/definitions/JiraSynchronization'
      userStatistic:
        $ref: '#/definitions/UserStatistic'

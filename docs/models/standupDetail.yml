definitions:
  StandupDetail:
    type: object
    properties:
      id:
        type: integer
      date:
        type: string
      standupProjectRating:
        $ref: '#/definitions/ProjectRating'
